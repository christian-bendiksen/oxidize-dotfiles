#!/usr/bin/env bash
waybar &
awww-daemon &
swayosd-server &
hypridle &
elephant &
walker --gapplication-service &
until awww query 2>/dev/null; do sleep 0.2; done
awww img "$(readlink -f "$HOME/.config/oxidize/themes/background")" --transition-type=none
